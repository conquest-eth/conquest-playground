{
  "name": "conquest-eth-web",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "fs-extra": "^10.1.0",
    "ipfs-gateway-emulator": "4.2.1-ipfs.2",
    "npm-run-all": "^4.1.5",
    "replace-in-file": "^6.3.2"
  },
  "scripts": {
    "dev": "node build.js && ipfs-emulator --only -d build -p 8080",
    "serve": "ipfs-emulator --only -d build -p 8080"
  }
}
